import { StatusBar } from 'expo-status-bar';
import { View } from 'react-native';

const Main: React.FC = () => {
  return (
    <View >
           <StatusBar style="auto" />
    </View>
  );
}

export default Main;